cors:
  open: true
  # allowed_origins:
  #   # - http://webhook.patrol.sh
  #   - http://localhost:5173
  

services:
  - name: service_a
    prefix: f
    destination: ${SERVICE_A_URL}
    
  - name: "service_b"
    prefix: s
    destination: ${SERVICE_B_URL}